import * as CT from '../../../Typings/CustomTypings.js';

export default (t: CT.Language) => ({
 0: t.JSON.defaultForumLayout[0],
 1: t.JSON.defaultForumLayout[1],
 2: t.JSON.defaultForumLayout[2],
});
